export interface DeviceProperties {
  eigenschaft: string;
  aktStatus: string;
}

export interface ReceivedDeviceProperties {
  applicationVersion: string;
  imageVersion: string;
  deviceType: string;
  os: string;
  javaVersion: string;
  applicationName: string;
}
